## 1. Workspace and service foundation

- [x] 1.1 Add the virtual workspace, seven member manifests, Rust 1.97 toolchain, `rustfmt.toml`, Platform lint levels and measured Clippy thresholds, `deny.toml`, crate-root safety attributes, and the exact SHA-pinned `ratatoskr-identifiers` dependency. No test: these are build and policy files; verify with `cargo metadata --locked --no-deps --format-version 1` after creating and inspecting `Cargo.lock`.
- [x] 1.2 Vet every newly added crate as required by `rust-security`, inspect all new `Cargo.lock` packages, and verify the candidate graph with `cargo deny --locked check advisories bans sources`. No test: dependency policy and lockfile generation.
- [x] 1.3 Add failing test `crates/core/tests/config.rs::defaults_are_finite_and_security_cannot_be_disabled`; assert the default admin bind is loopback, every required limit is finite and non-zero, allowed ports are 80 and 443, and the config has no policy-disable field. Run it and confirm the assertion fails because the scaffold defaults do not yet carry these values.
- [x] 1.4 Implement the typed `admin`, `blobs`, `fetch`, `shutdown`, and `telemetry` config tree plus built-in defaults and make 1.3 pass with `cargo test --locked -p ratatoskr-extractor-core defaults_are_finite_and_security_cannot_be_disabled`.
- [x] 1.5 Add failing test `crates/core/tests/config.rs::invalid_environment_is_reported_without_its_value`; load an unknown key and a wrong typed value containing `LEAKME`, and assert startup refuses both while the report names the environment key and never contains `LEAKME`. Run it and confirm the value currently leaks or the configuration is accepted.
- [x] 1.6 Implement the Platform-shaped Figment environment loader, semantic validation, stable value-free violations, and `check-config` exit classification; make 1.5 pass.
- [x] 1.7 Add failing test `services/extractor/tests/admin.rs::readiness_follows_startup_and_drain`; call all four admin routes around startup and drain transitions, and assert liveness stays 200, readiness is 503/200/503, metrics and version respond, and every response has `Cache-Control: no-store`. Run it and confirm the lifecycle assertions fail against the route stubs.
- [x] 1.8 Implement the service-owned admin router and atomic runtime health state, with no public fetch route, and make 1.7 pass.
- [x] 1.9 Add failing test `crates/telemetry/tests/redaction.rs::fetch_failure_telemetry_excludes_url_and_query_secret`; record a failure for a URL containing `LEAKME`, capture the structured record, and assert it contains only the failure class and no URL, query, path, header, body, or secret. Run it and confirm the current emission leaks or does not emit the required class.
- [x] 1.10 Implement the one-time tracing/OTLP/Prometheus bootstrap, closed field vocabulary, fetch metric names, and safe failure recording; make 1.9 pass.
- [x] 1.11 Add failing test `services/extractor/tests/shutdown.rs::shutdown_refuses_new_work_before_the_deadline`; keep one admitted operation open, start shutdown under paused Tokio time, and assert readiness fails immediately, later admission is refused, and the original work is cancelled at the one configured deadline. Run it and confirm one lifecycle assertion fails.
- [x] 1.12 Implement one admission state and bounded shutdown sequence owned by the service, join every spawned task, and make 1.11 pass.
- [x] 1.13 Add failing test `crates/test-support/tests/server.rs::scripted_server_records_requests_without_sleeping`; assert a local scripted server can return chunks, redirects, and stalls while exposing request count and headers without fixed ports or sleeps. Run it and confirm the harness does not yet produce the scripted exchange.
- [x] 1.14 Implement the test-only local HTTP server, scripted resolver, controllable time/jitter seams, and temporary blob root, and make 1.13 pass. Keep `ratatoskr-extractor-test-support` absent from every production dependency graph.

## 2. URL normalization, routing, and SSRF policy

- [x] 2.1 Add failing test `crates/url-routing/tests/normalization.rs::equivalent_tracked_urls_share_a_key_and_preserve_originals`; assert casing, default ports, fragments, and the documented tracking keys normalize away while other query pairs keep order and both original values remain intact. Run it and confirm normalized URLs or fingerprints differ.
- [x] 2.2 Implement conservative URL normalization and SHA-256 routing fingerprints and make 2.1 pass.
- [x] 2.3 Add failing test `crates/url-routing/tests/normalization.rs::ambiguous_urls_fail_before_resolution`; table unsupported schemes, missing hosts, user information, port zero, disallowed ports, overlong input, and alternative numeric host forms; assert a typed invalid-URL or policy error and zero resolver calls. Run it and confirm at least one case is accepted or resolved.
- [x] 2.4 Implement syntax, length, user-information, host, and configured-port validation and make 2.3 pass.
- [x] 2.5 Add failing test `crates/url-routing/tests/classification.rs::known_hosts_win_without_matching_lookalikes`; table delegated provider hosts, public adapter hosts, PDF paths, generic URLs, and suffix lookalikes, and assert the exact `SourceRoute`. Run it and confirm the classifier returns the scaffold route.
- [x] 2.6 Implement exact-host and dot-subdomain classification before generic web and make 2.5 pass.
- [x] 2.7 Add failing test `crates/url-routing/tests/policy.rs::every_non_global_address_class_is_denied`; cover IPv4, IPv6, IPv4-mapped IPv6, metadata endpoints, encoded/alternative forms, and public controls; assert each prohibited value maps to its bounded policy class without address text. Run it and confirm at least one prohibited address is accepted.
- [x] 2.8 Implement the closed address-range policy using standard-library address inspection plus explicit missing ranges and make 2.7 pass.
- [x] 2.9 Add failing test `crates/url-routing/tests/resolver.rs::a_mixed_dns_answer_is_denied_without_connectable_addresses`; script one public and one private result, assert a policy denial, no returned socket address, and no address text in the public report. Run it and confirm the public address is returned or the error is misclassified.
- [x] 2.10 Implement the validating Reqwest resolver and typed DNS/policy error propagation and make 2.9 pass.
- [x] 2.11 Add regression test `crates/url-routing/tests/resolver.rs::every_resolution_is_revalidated`; return a public answer first and a prohibited answer second for the same host, and assert the second call is denied rather than reusing authorization from the first. No separate RED: the minimum 2.10 implementation already validates each lookup and stores no authorization cache.
- [x] 2.12 Keep resolver authorization stateless across answers, leave transport-safe DNS caching to the underlying resolver, and make 2.11 pass.

## 3. Extractor-owned content-addressed artifacts

- [x] 3.1 Add failing test `crates/blob-store/tests/store.rs::stored_artifact_is_announced_by_matching_blob_ref`; store known bytes and assert the returned shared `BlobRef` owns them as `ratatoskr-extractor`, its SHA-256 and length match bytes read from the resolved internal path, and serialized output has no URL or path. Run it and confirm the reference or committed file does not match.
- [x] 3.2 Implement staging writes, incremental SHA-256, bounded length conversion, effective media type validation, atomic content-addressed commit, internal resolution, and `BlobRef` construction; make 3.1 pass. This completes workspace change tasks 3.3 and 3.4 only after their test evidence is observed.
- [x] 3.3 Add failing test `crates/blob-store/tests/store.rs::duplicate_bytes_reuse_one_verified_artifact`; write identical bytes concurrently and assert both references are equal, one valid target remains, and neither writer overwrites different bytes. Run it and confirm the duplicate path or verification fails.
- [x] 3.4 Make commit idempotent under concurrent identical writes, verify any pre-existing target, and make 3.3 pass.
- [x] 3.5 Add failing test `crates/blob-store/tests/store.rs::failed_stream_leaves_no_committed_or_staging_artifact`; inject a stream error after one chunk and assert no `BlobRef`, digest target, or staging file remains. Run it and confirm startup cleanup does not yet remove a stale service-created staging artifact.
- [x] 3.6 Implement cancellation/error cleanup and startup cleanup of stale service-created staging names without using source filenames, and make 3.5 pass.
- [x] 3.7 Add failing test `crates/blob-store/tests/verify.rs::missing_or_changed_cached_bytes_fail_verification`; remove and then alter referenced bytes, asserting distinct missing and digest/length mismatch errors. Run it and confirm either invalid artifact verifies.
- [x] 3.8 Implement owner, algorithm, digest, length, and media-type verification before a prior reference is reused and make 3.7 pass.

## 4. Streaming safe fetch

- [x] 4.1 Add failing unit test `crates/safe-fetch/src/tests.rs::one_response_is_streamed_to_one_matching_artifact`; serve a multi-chunk body, assert one request, bounded chunk processing, one artifact, matching `BlobRef`, final status/URL, byte counts, digest, and media evidence. It is a unit test so the loopback transport seam cannot be enabled in the production API. Run it and confirm the scaffold result fails.
- [x] 4.2 Implement the shared Reqwest/Rustls client with automatic redirects, cookies, ambient proxies, and transparent decompression disabled by the exact feature set; implement the single-hop streaming orchestration into `blob-store` and make 4.1 pass.
- [x] 4.3 Add failing unit test `crates/safe-fetch/src/tests.rs::redirect_target_is_revalidated_before_the_next_request`; redirect from a test-only trusted loopback origin to a prohibited target and assert a redirect policy error and one server request. Run it and confirm the redirect response is accepted as final.
- [x] 4.4 Implement bounded manual redirect history, relative `Location` joining, hop normalization, re-resolution, credential stripping by constructing each GET from the normalized target, and loop/hop-limit errors; make 4.3 pass.
- [x] 4.5 Add failing unit test `crates/safe-fetch/src/tests.rs::redirects_and_retries_share_one_deadline`; use paused time so the first hop consumes most of the budget, then assert the next phase ends at the original deadline with the correct timeout class. Run it and confirm the operation receives a fresh client timeout.
- [x] 4.6 Implement one monotonic total deadline shared by every hop, connect cap in the production client, and first-byte cap; body reads remain inside the same total deadline and 4.10 adds the explicit idle decoder boundary. Make 4.5 pass.
- [x] 4.7 Add failing unit test `crates/safe-fetch/src/tests.rs::declared_and_actual_wire_limits_stop_before_commit`; cover excessive `Content-Length` and a streamed body whose actual size exceeds the configured bound, assert the respective early/stream limit errors and no artifact. Run it and confirm either response commits or reads past the limit.
- [x] 4.8 Implement declared-length preflight plus actual wire-byte counting and make 4.7 pass.
- [x] 4.9 Add failing unit test `crates/safe-fetch/src/tests.rs::decoded_limit_stops_a_small_compressed_expansion`; serve a supported compressed body below the wire limit that expands above the decoded limit, assert `DecodedBodyTooLarge` and no artifact. Run it and confirm the expansion commits as encoded bytes.
- [x] 4.10 Implement bounded streaming decoders only for advertised supported encodings, explicit unsupported/malformed encoding errors, effective media sniffing, decoded-byte counting, and the configured read-idle boundary; make 4.9 pass.
- [x] 4.11 Add failing unit test `crates/safe-fetch/src/tests.rs::cache_validators_survive_without_sensitive_headers`; return `ETag`, `Last-Modified`, `Cache-Control`, `Set-Cookie`, `WWW-Authenticate`, and a secret query URL, then assert the allowed facts and required provenance exist while sensitive response headers do not. Run it and confirm missing evidence.
- [x] 4.12 Implement the closed response metadata projection, redirect evidence, attempt/cache outcome, and bounded metric updates and make 4.11 pass.
- [x] 4.13 Add failing unit test `crates/safe-fetch/src/tests.rs::not_modified_without_verified_bytes_is_an_integrity_error`; send validators and a missing prior `BlobRef`, return 304, and assert a typed cache-integrity error with no successful artifact. Run it and confirm 304 is accepted.
- [x] 4.14 Implement conditional headers and mandatory prior-artifact verification on 304 and make 4.13 pass.
- [x] 4.15 Add failing unit test `crates/safe-fetch/src/tests.rs::not_modified_reuses_one_verified_artifact`; provide a valid prior artifact, return 304, and assert `Revalidated`, the same reference and bytes, preserved validators, and no replacement write. Run it and confirm the verified artifact is not returned.
- [x] 4.16 Complete the verified revalidation success path and make 4.15 pass.
- [x] 4.17 Add failing unit test `crates/safe-fetch/src/tests.rs::policy_and_deterministic_failures_are_attempted_once`; table policy, encoding, body-limit, cache, and redirect errors and assert they are ineligible while transport is eligible. Run it and confirm the placeholder classifier retries deterministic failures.
- [x] 4.18 Implement the closed retry eligibility classifier and make 4.17 pass.
- [x] 4.19 Add failing unit test `crates/safe-fetch/src/tests.rs::eligible_get_retry_honors_retry_after_jitter_and_deadline`; script a transient result followed by success and assert exact attempts, bounded delay, original deadline, and final artifact. Run it and confirm attempts differ. Real sockets use real time; the pure deadline boundary remains covered under paused time by 4.5.
- [x] 4.20 Implement bounded GET retry, pure full-jitter and `Retry-After` calculation, attempt accounting, and deadline checks and make 4.19 pass.
- [x] 4.21 Add failing unit test `crates/safe-fetch/src/tests.rs::per_host_capacity_refuses_before_dns`; occupy the configured host permit, submit another fetch, and assert an overload error and zero transport calls. Run it and confirm work reaches transport.
- [x] 4.22 Implement finite global and per-host admission with immediate refusal and make 4.21 pass.

## 5. Process, deployment, and final gates

- [x] 5.1 Add failing test `services/extractor/tests/boot.rs::configured_process_serves_admin_only`; start the real binary with a temporary blob root and ephemeral admin port, assert `check-config` succeeds without binding, the running process becomes ready, exposes only the four admin routes, and exits cleanly on termination. Run it and confirm readiness never arrives.
- [x] 5.2 Wire validated configuration, blob-root preparation, telemetry, fetch stack construction, listener bind, readiness, and shutdown in `services/extractor`; make 5.1 pass without adding a command bus or public fetch endpoint.
- [x] 5.3 Add `deploy/systemd/ratatoskr-extractor.service`, example environment, deployment notes, service user/blob/log directory preparation, writable-path restriction, resource ceilings, and verification commands copied and adapted from Platform. No test: deployment configuration; `systemd-analyze` is not available on this macOS host, so the documented command remains the target-host check.
- [x] 5.4 Add `.github/workflows/ci.yml` and replace the proposed text in `DEVELOPMENT.md` with the exact ordered commands: `cargo fetch --locked`, `cargo deny --locked check`, format, Clippy all targets with warnings denied, debug build, workspace tests, release build, source-file length gate, and a check that the documented command list matches CI. No test: CI/documentation; the command-extraction check passed locally.
- [x] 5.5 Update `README.md` and `docs/IMPLEMENTATION_PLAN.md` to describe items 1 through 3 as implemented, the real crate/deploy layout, extractor-owned blob storage, and the deliberate deferral of Document IR and bus integration. No test: documentation; verify names and commands against the built tree.
- [x] 5.6 Mark only tasks 3.3 and 3.4 complete in workspace change `unblock-the-first-domain-service`; leave 3.1 and 3.2 pending for extractor plan item 6. No test: cross-repository planning status; `openspec instructions apply --change unblock-the-first-domain-service --store ratatoskr-workspace --json` reports those two tasks complete and 3.1/3.2 pending.
- [x] 5.7 Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo build --workspace --locked`, `cargo test --workspace --locked`, `cargo test --workspace --locked --doc`, `cargo deny --locked check`, `cargo tree --locked --duplicates`, the 850-line source gate, and `openspec validate implement-safe-fetch-foundation --strict`; every command completed successfully. The duplicate tree contains transitive major-version splits and `cargo deny` reports them as warnings while advisories, bans, licenses, and sources pass.
- [x] 5.8 Review the final diff for forbidden panics, unsafe code, secret-bearing telemetry, raw paths in `BlobRef`, ambient proxy/cookie/redirect behavior, unbounded queues or buffers, missing test RED evidence, unrelated changes, and Document IR/bus/database scope creep; `rg` finds no production `unwrap`, `expect`, `panic!`, `todo!`, `unimplemented!`, or unsafe blocks, and the scope audit finds no Document IR, bus, or database code.
- [x] 5.9 Commit only the intended extractor paths on `main` with repository-conventional metadata that does not name an assistant, push to `origin/main`, and verify local `HEAD`, remote `refs/heads/main`, clean status, and the final GitHub Actions gate. Commit `3b3a63c` is on `origin/main`; final ref and Actions verification follows this status-only commit.
